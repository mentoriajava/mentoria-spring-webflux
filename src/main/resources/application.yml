io:
  github:
    paulushcgcj:
      stub:
        company: ./stubs/companies_db.json
        user: ./stubs/users_db.json
      security:
        secret: iamasecurepassword
        expire: 5m

spring:
  main:
    banner-mode: "off"
  datasource:
    url: jdbc:mysql://127.0.0.1/mentoria?createDatabaseIfNotExist=true
    username: root
    password: root
  data:
    jpa:
      repositories:
        enabled: true
